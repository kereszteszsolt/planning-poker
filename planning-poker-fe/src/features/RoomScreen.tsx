import * as React from "react";

const RoomScreen: React.FC = () => {
  return (
    <div>
      <h1>Room Screen</h1>
      <p>
        This is the room screen where users can join or create rooms for
        planning poker sessions.
      </p>
    </div>
  );
};

export default RoomScreen;
